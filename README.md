# banking_etl
Production-grade banking ETL pipeline using Python, PostgreSQL, Airflow, and Docker. Extracts, transforms, and loads transactional data into a star-schema warehouse with staging, data quality checks, audit metrics, and fully containerized orchestration via Docker Compose.
